

def load_shadowdb():
    # Get all profiles
    # for profile in profiles
    #   write shadow.save(partial-profile) profile.id, profile.content.names
    #   profile.get_notes
    #   for paper in papers:
    #     get authors
    #     for author in authors:
    #         if authorid is email:
    #            fetch profile for email
    #            create union between fetched profile.id and email (if different)
    pass
